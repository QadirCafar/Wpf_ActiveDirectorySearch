<Window x:Class="ActiveDirectorySearch.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ActiveDirectorySearch"
        mc:Ignorable="d"
        Title="Disc searcher" Height="450" Width="800">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- ComboBox for Drives -->
        <ComboBox x:Name="DriveSelector" Margin="5" Height="30"/>

        <!-- DataGrid for Results -->
        <DataGrid x:Name="ResultsGrid" Grid.Row="1" AutoGenerateColumns="False" Margin="5" HeadersVisibility="Column">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Path" Binding="{Binding Path}"/>
                <DataGridTextColumn Header="File Count" Binding="{Binding FileCount}"/>
                <DataGridTextColumn Header="Size (MB)" Binding="{Binding TotalSizeMB}"/>
            </DataGrid.Columns>
        </DataGrid>

        <!-- Buttons -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center" Margin="5">
            <Button x:Name="StartButton" Content="Search" Margin="5" Width="120" Click="StartButton_Click"/>
            <Button x:Name="PauseButton" Content="Pause" Margin="5" Width="120" Click="PauseButton_Click"/>
            <Button x:Name="ResumeButton" Content="Resume" Margin="5" Width="120" Click="ResumeButton_Click"/>
        </StackPanel>
    </Grid>
</Window>
